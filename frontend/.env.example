# Use the local FastAPI backend. Set true only for the standalone mock demo.
VITE_USE_MOCK_DATA=false
VITE_API_BASE_URL=http://127.0.0.1:8000
# Optional: defaults to E0001 with the API, EMP_001 with mock data.
# VITE_DEFAULT_EMPLOYEE_ID=E0001
